/*
Questão 20 - Qual é a utilidade de forEach() se ele não retornar nenhum valor?
*/

Embora o método `forEach()` não retorne um valor, ele é amplamente utilizado em JavaScript por sua utilidade em realizar operações em cada elemento de um array. Aqui estão algumas das razões pelas quais o `forEach()` é útil:

1. **Iteração sobre elementos**: `forEach()` é uma maneira conveniente de iterar sobre cada elemento de um array e executar uma operação para cada um deles.

2. **Legibilidade**: O uso de `forEach()` pode tornar o código mais legível e expressivo, especialmente quando você precisa realizar uma operação para cada elemento de um array.

3. **Manutenção de estado**: Você pode usar `forEach()` para atualizar variáveis externas ou realizar outras operações que dependem do estado atual do código.

4. **Clareza de intenção**: Usar `forEach()` comunica claramente a intenção de iterar sobre os elementos de um array, tornando o código mais compreensível para outros desenvolvedores.

5. **Compatibilidade**: O método `forEach()` é suportado em todos os navegadores modernos e é uma maneira confiável de iterar sobre elementos de um array sem a necessidade de usar construções mais antigas, como loops `for`.

Embora `forEach()` não retorne um valor, sua utilidade em iterar sobre elementos de um array e realizar operações para cada um deles o torna uma ferramenta valiosa para a programação em JavaScript.